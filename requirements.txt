# Pinned to the exact resolved, conflict-free set (Chainlit constrains fastapi/
# uvicorn/packaging, so those are pinned to what it accepts).
# Backend
fastapi==0.115.14
uvicorn==0.25.0
starlette==0.41.3
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# LLM provider (OpenAI-compatible SDK; points at NVIDIA NIM by default)
openai==2.46.0

# UI
chainlit==2.0.4

# Data / artifacts
pandas==2.2.3
numpy==2.5.1
matplotlib==3.10.0
tabulate==0.9.0

# Tests
pytest==8.3.4

# Pinned because Chainlit requires packaging<24
packaging==23.2
